Copy these files into the root of your local repository and replace the existing files.

Then run these commands from the repo root:

python -m sphinx.ext.apidoc -o docs/source news_app
python -m sphinx -b html docs/source docs/build/html

git add .
git commit -m "Expand docstrings and improve README for resubmission"
git push

After pushing, submit capstone.txt again.
